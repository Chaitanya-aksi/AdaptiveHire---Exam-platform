# Backend REST base URL (matches PORT in the repo-root .env).
VITE_API_URL=http://localhost:3001/api

# WebSocket origin for the proctoring gateway (built in step 4).
VITE_WS_URL=http://localhost:3001
